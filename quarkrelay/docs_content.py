"""自动生成，请勿手改 —— 由 scripts/build-docs.py 从 FEATURES.md / RELEASE_NOTES.md 生成。

改文案请改仓库根目录的那两个 .md 文件，然后重新运行：
    python scripts/build-docs.py
"""

FEATURES = '# 夸克中转站 · 完整功能清单\n\n> 这份清单写得比较全，几乎不怎么变；每次版本更新只改 `RELEASE_NOTES.md`。\n> 首次安装的朋友看这份，老用户升级只看当版公告就行。\n\n---\n\n## 一、夸克中转站（核心）\n\n**输入**\n\n* 粘贴框支持整段文字，自动识别其中的分享链接与提取码\n* 一次可粘贴多条链接，批量处理\n* 提取码来源三路启发式：URL 参数（`?pwd=`）、「提取码：」等前缀、括号内四位\n* 「读取剪贴板」一键取内容\n* 剪贴板监听到夸克链接时自动填充（可在设置里关掉）\n* 链接识别覆盖：夸克、UC、百度、阿里云盘、123 云盘、天翼、迅雷、115\n\n**转存**\n\n* 目标目录支持多级路径自动创建\n* 转存后自动定位文件 ID（开放平台无目录列举接口，用文件名搜回来）\n* 转存任务状态轮询，失败会告诉你到底是空间不足还是分享失效\n\n**生成分享**\n\n* 分享名字自定义，留空自动用分享标题／文件名\n* 有效期：永久 / 1天 / 7天 / 30天 / 60天 / 100天 / 180天\n* 分享方式：私密（带提取码）/ 公开（无提取码）\n* 结果每条可选：复制名字、复制链接、复制全部（名字+链接）\n* 组合结果预览 + 一键复制全部\n\n---\n\n## 二、截图识链（页内小按钮）\n\n截图识链不占独立页面，而是一个 18px 的**相机小图标**，长在需要输入链接的页面右上角：\n「夸克中转」页识别夸克链接，「跨盘搬运」页按当前方向识别百度或夸克链接，\n识别结果直接填进该页的输入框。左键框选，右键选更多取图方式。\n\n**取图方式**\n\n* 框选识别（左键默认）：跨屏幕全屏遮罩，拖框即识别，Esc 取消，Enter 确认\n* 整屏识别：多显示器自动拼接\n* 打开图片：png / jpg / bmp / webp / gif\n* 读剪贴板：图片优先，其次文本\n\n**识别引擎**\n\n* Windows.Media.Ocr 系统 OCR（离线、免费、不用下模型）\n  按 `zh-Hans-CN → zh-Hans → zh-CN → en-US` 顺序挑可用语言\n* OpenCV QRCodeDetector：原图 + 2x/3x/0.5x 缩放 + OTSU / 自适应阈值三轮回退\n* 两路结果合并去重，分别标注来源（二维码 / 系统 OCR / 剪贴板）\n\n**入口**\n\n* 页内小相机按钮（左键框选 / 右键菜单）\n* 全局快捷键 `Ctrl+Alt+Q`：作用于当前页面\n* 系统托盘菜单「截图识链」：同上\n\n---\n\n## 三、跨盘搬运（双向）\n\n一个页面、一个方向开关，两条链路都是「转存到自己的中转目录 → 取直链或流式下载 → 上传对方网盘」。\n\n**百度 → 夸克**\n\n* 解析：分享链接 + 提取码自动识别（`/s/1xxxx` 与 `?surl=` 两种形态都认），\n  浏览器自动带提取码校验（`share/verify`），解析出文件名 / 大小 / 是否目录\n* 勾选要搬的文件，转存到自己的百度中转目录（自动建目录）\n* 直链由内置浏览器截获（见下），流式下载到 `%APPDATA%\\QuarkRelay\\cache\\buffer\\`（**不落桌面**）\n* 支持断点续传（`.part` 式增量写入 + Range）\n* 下载完直接上传夸克（官方分片直传，多分片、多进度），可选生成夸克分享链接\n\n**夸克 → 百度**\n\n* 解析夸克分享并预览内容（该方向为整包转存）\n* 转存到自己的夸克中转目录（开放平台无目录列举接口，用文件名搜回 fid）\n* 流式下载后上传百度网盘：预上传（支持秒传）→ 4MB 分片 → 收尾\n* 同名文件可选「自动加序号 / 跳过 / 覆盖」\n* 可选搬运完成后生成百度分享链接（有效期跟随设置）\n\n**直链获取（内置浏览器）**\n\n* QtWebEngine 隐藏页面打开百度网盘对应目录\n* 注入脚本自动寻找「下载」按钮并点击\n* 从 `downloadRequested` 截获真实直链\n* 自动点击失败会把窗口弹出来，你手点一下即可\n* 兜底：也可以手动粘贴 dlink\n\n---\n\n## 四、账号与登录\n\n* 夸克：官方开放平台授权码流程（OAuth），**后台轮询授权结果**（等授权 → 换令牌 → 取用户信息\n  一条龙），授权期间实时显示状态，拿到授权码自动保存登录；access_token 自动刷新\n* 百度：内置浏览器登录，读取 HttpOnly Cookie（BDUSS / STOKEN / PANWEB / BAIDUID）\n* Cookie 收集用「**静默窗口**」判定（最少 700ms、连续 400ms 无新 Cookie 才收工，最长 8 秒），\n  Cookie 多也不会漏；建会话时按域名取值，避免同名 Cookie 互相覆盖\n* 两个 Profile 各自独立持久化，与系统浏览器完全隔离\n* 账号页展示昵称、夸克会员类型与容量占用\n* 一键退出 / 一键清除全部凭据\n\n---\n\n## 五、任务中心\n\n* 线程池并发（默认 2，可调 1~6）\n* 实时阶段文字 + 进度条 + 速度\n* 单条取消 / 全部取消 / 清空已结束\n* 内存日志面板（最近 3000 条），同时落盘到 `logs\\`\n\n---\n\n## 六、历史记录\n\n* SQLite 持久化：时间、类型、名字、链接、提取码、来源、目录、备注\n* 关键字搜索\n* 批量复制「名字 + 链接」\n* 导出 CSV（UTF-8 BOM，Excel 直接打开不乱码）\n* 一键清空 / 自动裁剪（默认保留 1000 条）\n\n---\n\n## 七、设置\n\n* 夸克默认转存目录\n* 分享有效期与分享方式默认值\n* 命名模板（含五种预设与实时预览）\n* 并发任务数、下载限速（KB/s）\n* 剪贴板监听开关\n* 完成后自动复制开关\n* 完成后提示开关\n* **启动时自动检查更新 / 发现新版本自动下载安装**（+「立即检查更新」）\n* 深浅色主题\n* 关闭窗口是否最小化到托盘\n* 打开数据目录 / 清空历史 / 清除登录凭据\n\n---\n\n## 八、关于与自动更新\n\n* 版本、更新公告、完整功能清单三个页签\n* 「检查更新」读 GitHub 上的最新 Release，按数字段比对版本\n* 展示新版本号、安装包大小、发布日期与该版本的公告\n* 「立即更新」：后台带进度下载 → 校验（大小 + `MZ` 文件头 + 可选 sha256）→\n  生成替换脚本 → 退出后自动替换并重启，全程不用去网页下载\n* 默认启动静默检查；发现新版本且没有任务在跑时自动下载安装（有任务则推迟）\n* 程序目录不可写（如装在 `Program Files`）时自动降级为「去下载页」\n* 断网时只提示「检查更新失败」，绝不阻塞启动\n\n---\n\n## 九、其它\n\n* 系统托盘图标 + 右键菜单（显示窗口 / 截图识链 / 打开数据目录 / 退出）\n* 全局快捷键 `Ctrl+Alt+Q` 唤出当前页面的截图识链\n* 单实例运行（命名互斥体）\n* 未捕获异常会写日志并弹窗提示，附带日志目录路径\n* 高 DPI 适配（Per-Monitor V2）\n* 明暗两套主题，全部走同一套设计 token\n\n---\n\n## 十、技术栈\n\n| 层 | 用了什么 |\n| --- | --- |\n| 界面 | PySide6 (Qt 6 Widgets)，手写 QSS 设计系统 |\n| 浏览器 | QtWebEngine（命名持久化 Profile + CookieStore + downloadRequested 拦截）|\n| 夸克 | 开放平台 HTTP API，SHA256 请求签名（**只签路径**），OAuth 授权码换令牌 |\n| 百度 | 网页版接口：gettemplatevariable / share/verify / share/list / share/transfer / `api/list` / `api/create` / `api/precreate` / `superfile2` / `share/set` |\n| OCR | Windows.Media.Ocr（PowerShell WinRT 桥）|\n| 二维码 | OpenCV QRCodeDetector |\n| 更新 | GitHub Releases API + 校验下载 + 批处理自替换重启 |\n| 存储 | JSON 配置 + SQLite 历史 |\n| 打包 | PyInstaller 单文件 |\n'

RELEASE_NOTES = '# 夸克中转站 v1.1.0\n\n**发布日期：2026-09-13**\n\n这一版解决三件让你用不下去的事：**夸克登不上**、**登录信息读不全**、**只能单向搬运**，\n顺手把「检查更新」做成了真正的一键自动更新。\n\n---\n\n## 🔧 重要修复：夸克网盘登录不了\n\n之前点「登录夸克网盘」，扫码、点同意，然后就没有然后了。两个原因叠在一起：\n\n1. **请求签名算错了** —— 夸克开放平台的签名只覆盖「方法 + 路径 + 时间戳 + 密钥」，\n   而旧代码把 `?page_code=xxx` 这部分查询串也一起签了进去，服务端直接判定\n   `errno 10001 签名验证失败`。现在签名统一只算路径，这个坑不可能再踩。\n2. **构造错误对象时参数名写错** —— 抛异常时用了 `AppError(errno=...)`，而这个参数并不存在，\n   一进轮询就抛 `TypeError` 直接崩掉，日志里只有一个看不懂的类型错误。\n\n同时把授权流程改稳了：\n\n* 授权结果由**后台线程统一轮询**（等授权 → 换令牌 → 取用户信息一条龙），\n  窗口不再是「点一下就当失败」；\n* 授权期间接口返回「还没授权」不再被当成错误，状态栏会同步显示\n  「已扫码，等待你点击同意授权」这类实时提示；\n* 拿到授权码立即自动关闭窗口并保存登录状态，不需要你再点「我已授权完成」。\n\n## 🔧 重要修复：登录信息读取不到位\n\n* 读 Cookie 从「固定等 0.9 秒」改成**等回调流真正静默下来**（最少 700ms、连续 400ms 没新\n  Cookie 才收工，最长 8 秒）。Cookie 一多就不会再漏读，\n  `BDUSS` / `STOKEN` 这类 **HttpOnly** 的关键项也拿得全。\n* 建立百度会话时**按域名取 Cookie**。`BDUSS` 在 `.baidu.com` 和 `pan.baidu.com` 下可能\n  是两个不同的值，以前混在一起互相覆盖，登录看着「成功」了但接口就是不通。\n* 修掉了 WebEngine 缓存目录不存在导致的 `Unable to move the cache`，顺手把目录预建好。\n\n---\n\n## 🚀 新增：跨盘搬运支持双向\n\n「百度 → 夸克」页面升级成 **跨盘搬运** 页，顶部一个方向开关：\n\n* **百度 → 夸克**：解析分享 → 勾选文件 → 转存到你的百度中转目录 →\n  内置浏览器截获直链 → 流式下载 → 上传夸克 → 可选生成夸克分享链接\n* **夸克 → 百度**：解析分享（整包预览）→ 转存到你的夸克中转目录 → 流式下载 →\n  上传百度网盘（同名文件自动加序号，也可选择跳过或覆盖）→ 可选生成百度分享链接\n\n两个方向都不经过桌面：文件只在 `%APPDATA%\\QuarkRelay\\cache\\buffer\\` 里过一手，\n任务结束随用随删（想留着可以勾「保留下载缓冲文件」）。\n\n## 🚀 新增：一键自动更新\n\n「关于」页现在是完整的更新中心：\n\n* 读取 GitHub 上的**最新 Release**，和本地版本按数字段比对（`1.10.0` 比 `1.9.9` 新）\n* 显示新版本号、**安装包大小**、发布日期，并把该版本的更新公告单独列一个页签\n* 点「立即更新到 vX.Y.Z」→ 后台带进度下载 → 自动校验（大小 + `MZ` 文件头 + 可选的 sha256）\n  → 生成替换脚本 → **重启后就是新版本**，全程不用去网页下载\n* 默认**启动时静默检查**；发现新版本且当前没有任务在跑时，会**自动下载并安装**\n  （有任务在跑就推迟，绝不打断你正在搬的东西，可在设置页关掉）\n* 装在 `Program Files` 这类不可写目录时不会硬来，退化成「去下载页」按钮\n\n## 🚀 改进：截图识链不再是独立页面\n\n截图识链以前占了一个侧边栏页面，你得先在那边截好、再「送到夸克中转站」，链路太长。\n现在它变成了一个 **18px 的相机小图标**，直接长在需要输入链接的页面上：\n\n* **夸克中转页**右上角：截图识别到的夸克链接直接填进输入框\n* **跨盘搬运页**右上角：按当前方向识别百度或夸克链接，自动填好链接与提取码\n\n左键点一下就是**框选识别**（最常用），右键弹出「整屏识别 / 读剪贴板 / 打开图片…」。\n全局快捷键 `Ctrl+Alt+Q` 和托盘菜单里的「截图识链」现在都作用于**当前页面**，\n在哪一页就识别哪种链接。\n\n---\n\n## ✨ 其它改动\n\n* 设置页新增「版本更新」区：启动自动检查、自动下载安装两个开关 + 「立即检查更新」\n* 自检脚本从 10 项扩到 11 项，新增「检查更新：版本比较与降级路径」，断网也不会误报失败\n* 补上百度网盘的上传实现（预上传秒传 → 4MB 分片 → 收尾）与分享创建（`share/set`）\n\n## 📦 安装\n\n下载本页附件里的 `QuarkRelay.exe`，双击运行即可。已经装了旧版本的，\n放到同一位置覆盖，或者直接在程序里点「关于 → 检查更新」。\n\n数据全部保存在 `%APPDATA%\\QuarkRelay\\`，卸载时直接删掉这个目录即可。\n\n## ⚠️ 已知限制\n\n* 百度搬运链路依赖百度网盘网页版结构；改版时可能需要你手点一下「下载」按钮\n* 百度分享里的**目录**暂不支持整目录搬运，请勾选具体文件\n* 夸克 → 百度方向是**整包转存**（夸克分享的勾选暂不支持），搬运前可以在预览里确认内容\n* 自动替换依赖程序目录可写；装在需要管理员权限的目录时会提示手动下载\n* 阿里云盘 / 123 云盘 / 天翼等链接已经能识别分类，但转存还没接通，后续版本补上\n\n## 🙏 说明\n\n本工具只用于管理你自己账号里的文件、转存他人公开分享的合法内容。\n请勿用于传播盗版或侵权内容。\n'

__all__ = ["FEATURES", "RELEASE_NOTES"]
