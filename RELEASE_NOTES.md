# 夸克中转站 v1.1.4 更新公告

| 项目 | 内容 |
| --- | --- |
| 发布日期 | 2026-09-14 |
| 上一版本 | v1.1.3 |
| 适用平台 | Windows 10 / 11（64 位） |
| 安装包 | `QuarkRelay.exe`（单文件免安装，约 262 MB） |
| 升级方式 | 覆盖旧文件，或在程序内「关于 → 检查更新 → 立即更新」 |

## 摘要

本版修复一个**致命问题：夸克分享解析全线失败**。9 月中旬起，所有「转存并分享」「夸克 → 百度」
任务都会在第一步「解析分享链接」报「签名验证失败，禁止访问」；本版把分享解析改走另一条通道，
其余环节（转存、定位、生成分享）不变，登录凭据无需重做。

* 修复 1 个核心缺陷：夸克官方悄悄撤销了开放平台的两条「读分享」接口，本版改走网页版的
  「客态」接口拿分享标题与文件列表，转存仍走原通道；
* 顺手改进：分享打不开时的报错从「签名验证失败，禁止访问」这种人话翻译不出来的内容，
  换成「分享不存在或已被删除」「提取码不正确」「分享已过期」等能看懂的提示；
* 不涉及配置项、历史记录、登录凭据的存储格式变更，可直接覆盖升级；
* 自检仍为 13 项（本版未新增断言）。

---

## 一、缺陷修复

### 1.1 夸克分享解析失败：「签名验证失败，禁止访问」

**调整前的实际行为**

从某一天起（本机日志里 9 月 13 日 17 点起全部如此），粘贴任何夸克分享链接点转存，
任务都立刻失败，报错是「签名验证失败，禁止访问」。这不是签名真错了：

* 用同一套签名去调用户信息、转存、任务查询等接口都正常；
* 唯独「解析分享」的两条接口（`share/detail`、`share/page_detail`）怎么调都报这个错，
  而且和「随便编一个不存在的路径」得到的响应一模一样——**是夸克官方把这两条路由撤销了**，
  网关拿「签名验证失败」当幌子而已。

**现在的行为**

分享解析改走**网页版夸克网盘的「客态」接口**（就是浏览器打开分享页用的那套，不需要登录
也不需要签名）：

1. 先拿分享的 `stoken` 与标题；
2. 再列出分享里的文件（支持进子目录，供「夸克 → 百度」方向预览）；
3. 转存仍走原来的开放平台通道，两边的 token 已实测互通。

对用户来说操作完全不变：粘贴链接 → 转存 → 生成分享，一步不多。

### 1.2 报错内容翻译

客态接口的错误码和开放平台不是一套，本版补了翻译：分享被删、提取码不对、分享过期，
现在都会以人话提示，而不是甩一句「签名验证失败」或「错误码 41006」。

---

## 二、新增或界面调整

本版无新增功能与界面调整。

---

## 三、其它改动

* `quarkrelay/core/quark.py`：新增客态接口调用层（`_web_share`）与文件名字段归一化
  （网页接口用 `file_name`，转成内部统一的 `filename`，调用方零改动）；
* 返回给界面的数据结构与之前完全一致，`夸克中转`、`夸克 → 百度` 两个方向都不用改界面代码。

---

## 四、兼容性

* 配置项、历史记录、登录凭据的存储格式本版未变更，夸克 / 百度登录都无需重做；
* 分享解析换了通道，但解析结果（标题、文件列表）与之前一致，历史记录不受影响。

---

## 五、验证情况

* 程序自检 13 项全部通过；
* 用真实分享端到端实测（经应用自身的转存流水线）：
  * 解析 → 建目录 → 转存 → 定位文件 → 生成分享链接，全流程成功；
  * 分享内子目录可正常列出；
  * 不存在的分享返回「分享不存在或已被删除」，不再是误导性的「签名验证失败」；
  * 测试产生的文件与目录已从网盘清理。

---

## 六、已知限制（本版未处理）

* 客态接口是网页版接口，万一夸克再调整它，解析仍可能受影响（届时需要再适配）；
* 其余限制与 v1.1.3 相同：百度方向依赖网页版结构、夸克方向为整包转存、
  百度分享中的目录不支持整目录搬运、截图识别依赖 Windows 中文语言包等。

---

## 七、升级方式与数据位置

1. 程序内：关于 → 检查更新 → 立即更新（自动下载、校验、替换、清理并重启）。
2. 手动：下载本页附件的 `QuarkRelay.exe`，覆盖旧文件即可。

数据保存在 `%APPDATA%\QuarkRelay\`，覆盖或删除程序文件均不影响其中数据；
如需完全清理，删除该目录即可。

---

## 八、附：本版改动文件

| 文件 | 说明 |
| --- | --- |
| `quarkrelay/core/quark.py` | 分享解析改走网页客态接口；新增错误码翻译与字段归一化；`share_saveas` 等其余接口不变 |
| `quarkrelay/__init__.py` | 版本号 1.1.3 → 1.1.4 |
| `RELEASE_NOTES.md` | 本公告 |

---

## 免责声明

本工具只用于管理你本人账号中的文件，以及转存他人公开分享的合法内容。
请勿用于传播盗版、侵权或任何违法内容。使用本工具产生的一切后果由使用者自行承担。
