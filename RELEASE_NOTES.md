# 夸克中转站 v1.1.1

**发布日期：2026-09-13**

这一版只做一件事，但很实在：**登录时把二维码单独摆出来 —— 放大、留白、能扫。**

---

## 🔍 登录窗口左边多了一块二维码

以前点「登录夸克网盘」或「登录百度网盘」，弹出来的是整个登录网页，
二维码小小一枚挤在页面角落里，抬手去扫还得凑近屏幕、躲开页面上的浮层。
现在登录窗口左边多出一块 400px 的白底卡片：

* 从登录页里**取出**那枚二维码（夸克是 `<svg>`、百度是 `<img>`）单独展示，
  和右边网页里的**是同一枚**，页面刷新或换码时会跟着自动更新（约 1.2 秒一轮）
* 四周补上了二维码规范要求的**静区**（一小圈白边）—— 这两家的码都是**紧贴边缘**
  生成的，少了这圈留白，不少扫码器会直接认不出来
* 位图直接读像素；矢量图在本机按 3 倍超采样光栅化（关掉抗锯齿），
  缩回卡片尺寸后格子边缘依然干净
* 取不到码时不会留一块空白，会告诉你「多半是浏览器里已经登录了，直接在右边操作即可」
* 码过期了点一下「刷新二维码」：先替你在页面里点刷新，点不到就把整页重载
* 百度登录窗现在直接打开 passport 的**扫码页**，不用再自己进首页找「登录」按钮

## ✨ 其它改动

* 自检脚本从 11 项扩到 12 项，新增「登录二维码：抠图 → 静区 → 放大面板」：
  用一个假页面把「注入取码器 → 探测 → 取图 → 上屏」整条链路跑一遍，
  不联网、不弹窗
* exe 的 `--check` 自检里也加了一项二维码：确认 `PySide6.QtSvg` 确实被打进了包里，
  免得打包后只留下一句「取到了但没解析成功」
* 打包脚本显式带上 `PySide6.QtSvg`，自检输出里会打印二维码卡片的尺寸

## 📦 安装

下载本页附件里的 `QuarkRelay.exe`，双击运行即可。已经装了旧版本的，放到同一位置覆盖，
或者直接在程序里点「关于 → 检查更新」。

数据全部保存在 `%APPDATA%\QuarkRelay\`，卸载时直接删掉这个目录即可。

## ⚠️ 已知限制

* 百度搬运链路依赖百度网盘网页版结构；改版时可能需要你手点一下「下载」按钮
* 百度分享里的**目录**暂不支持整目录搬运，请勾选具体文件
* 夸克 → 百度方向是**整包转存**（夸克分享的勾选暂不支持），搬运前可以在预览里确认内容
* 自动替换依赖程序目录可写；装在需要管理员权限的目录时会提示手动下载

## 🙏 说明

本工具只用于管理你自己账号里的文件、转存他人公开分享的合法内容。
请勿用于传播盗版或侵权内容。
