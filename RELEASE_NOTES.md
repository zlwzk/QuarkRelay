# 夸克中转站 v1.1.3 更新公告

| 项目 | 内容 |
| --- | --- |
| 发布日期 | 2026-09-13 |
| 上一版本 | v1.1.2 |
| 适用平台 | Windows 10 / 11（64 位） |
| 安装包 | `QuarkRelay.exe`（单文件免安装，约 262 MB） |
| 升级方式 | 覆盖旧文件，或在程序内「关于 → 检查更新 → 立即更新」 |

## 摘要

本版是把「自动更新的收尾」补完整，不新增用户要操作的功能：

* 新增 1 项行为：自动替换完成后，先删掉旧版本的 exe、再清掉下载下来的安装包与 `.part`
  半成品，然后才打开新版本；新进程没起来会隔几秒重试两次；
* 新增 1 项兜底：替换脚本没跑完（程序被强杀、替换失败）时，下次启动会把上一版的残留
  再清一遍，并在界面上提示清理结果；
* 新增 1 处记录：`%APPDATA%\QuarkRelay\update\update.log` 记下每次替换的结果
  （成功 / 失败 / 已替换但没能打开）；
* 不涉及配置项、历史记录、登录凭据的存储格式变更，可直接覆盖升级；
* 自检仍为 13 项（在「更新」一项里扩展了断言，没有新增项）。

---

## 一、缺陷修复

本版无缺陷修复。

---

## 二、新增或界面调整

### 2.1 更新完成后自动删除旧版本文件与安装包，再打开新版本

**调整前的实际行为**

替换脚本本来就删旧版本文件、也把下载好的包挪到了程序位置，但收尾这一步不够结实：

* 旧版本文件（`QuarkRelay.exe.old`）只尝试删一次，被资源管理器或杀毒软件占着就失败，
  文件会一直留在程序目录里；
* 更新目录里只处理了「本次下载的那一个包」；上一次没装上的包、下到一半的 `.part`
  半成品不会清，一个包 262 MB；
* 新版本启动只 `start` 一次，没有任何确认：旧实例没退干净时，新实例会被互斥体挡回去，
  结果是「更新完了但程序没打开」，且没有任何提示或记录。

**现在的行为（严格按此顺序）**

1. 等旧进程退出（最长约 60 秒），把旧 exe 备份为 `*.old`，把新版本挪进来，失败最多重试 30 次；
2. **删除旧版本的 exe**：最多尝试 10 次、每次间隔 1 秒，专治「刚退出的文件还被占着」；
3. **清理安装包残留**：删掉更新目录里的 `*.part`、`*.exe`（下载下来的安装包）与
   `old-pid.txt`（更新目录是程序私有目录，只放这些临时文件；若程序本身就跑在更新目录里，
   会跳过包清理以免删掉自己）；
4. 往 `update.log` 追加一条记录：`替换完成：旧版本文件与安装包已清理，接下来打开 …`；
5. **打开新版本**，并确认进程真的起来了：最多尝试 3 次、每次隔约 2 秒；
6. 三次都没起来，再记一条 `已替换成新版本，但没能自动打开，请手动双击 …`。

顺序刻意是「先删旧的、再打开新的」。

### 2.2 启动时兜底再清一遍

替换脚本没机会跑完的情况（程序被强杀、替换失败、断电）以前会留下垃圾，现在程序每次
启动都会调 `updater.cleanup_after_update()` 兜底：

* 删掉程序目录里的 `QuarkRelay.exe.old`（最多 3 次、每次间隔 0.3 秒，删不掉就留到下次启动）；
* 清空更新目录里除 `update.log` 之外的所有文件（安装包、`.part`、替换脚本、临时文件）；
* **正在进行的更新不会被误伤**：如果 `apply-update.bat` 还在、且是 5 分钟内生成的，
  说明替换可能正在收尾，本次跳过清理；
* 清理结果会写进程序日志，并在界面上用一条提示告诉用户
  「更新完成：已清理上一版本文件与安装包（N 项，X MB）」——提示受「完成后提醒」开关控制。

### 2.3 文案

* 关于页更新说明改为：「发现新版本时可以在这里一键下载，程序重启后会替换成新版本、
  删掉旧版本文件与下载下来的安装包，然后自动打开新版本」；
* 点「立即更新」后的提示改为「更新已就绪，正在重启替换并清理旧文件」。

---

## 三、其它改动

* 自检「更新：版本比较与降级路径」一项里补了断言（自检项数不变，仍是 13 项）：
  * 替换脚本跑完后，更新目录里遗留的旧安装包与 `.part` 必须消失；
  * `update.log` 必须留下「替换完成」这条记录；
  * 兜底清理必须删掉旧版本备份、安装包与半成品，同时**不能**删掉当前版本与 `update.log`；
  * `apply-update.bat` 存在（更新进行中）时必须跳过清理，进行中的包不能被删；
  * 替换脚本模板的静态校验：`goto` 到的每个标签都必须真实存在，「打开新版本 → 确认进程
    起来了 → 起不来就留一条记录」这一段不能被改丢（真启动会弹窗，自检里不实际跑这一步）。
* `README.md`、`FEATURES.md` 同步补上收尾清理与 `update\` 目录的说明。

---

## 四、兼容性

* 配置项、历史记录、登录凭据的存储格式本版未变更，夸克 / 百度登录都无需重做；
* `%APPDATA%\QuarkRelay\update\` 会在更新完成后被清空（只保留 `update.log`），
  这是程序私有目录，不影响任何用户数据；
* 本版起旧版本文件与安装包会被自动删除，如仍需保留旧版 exe，请自行另存一份。

---

## 五、验证情况

* 程序自检 13 项全部通过，其中更新一项覆盖了「替换 → 删旧版本 → 清安装包 → 留记录」全流程。
* 对 v1.1.2 两次真实自动更新的现场核对（本机，程序放在桌面）：
  * 5 次更新前后的日志显示「更新包已就绪 → 替换脚本已启动 → 约 25 秒后新进程启动」；
  * 程序目录里只剩当前版本的 `QuarkRelay.exe`，没有 `*.old` 残留；
  * `%APPDATA%\QuarkRelay\update\` 为空（包已挪走、脚本已自删）。
* 本版新增的五组断言（删旧安装包 / 留记录 / 兜底清理 / 进行中跳过 / 脚本模板静态校验）
  均在新自检中跑通。

---

## 六、已知限制（本版未处理）

* 删除失败（文件被杀软或资源管理器占用）时不会阻塞流程：替换脚本最多试 10 次，
  仍失败就留给下次启动的兜底清理重试 3 次；极端情况下（一直被占用）文件会留在
  程序目录里，直到某次启动时能删掉。
* 兜底清理只在程序启动时执行，程序长时间不重启则不会清理。
* 走「手动下载」路径时（程序目录不可写，例如装在 `Program Files`），下载下来的包在
  你的下载目录里，属于系统下载目录，程序不会去动它。
* 其余限制与 v1.1.2 相同：百度方向依赖网页版结构、夸克方向为整包转存、
  百度分享中的目录不支持整目录搬运、截图识别依赖 Windows 中文语言包等。

---

## 七、升级方式与数据位置

1. 程序内：关于 → 检查更新 → 立即更新（自动下载、校验、替换、清理并重启）。
2. 手动：下载本页附件的 `QuarkRelay.exe`，覆盖旧文件即可。

数据保存在 `%APPDATA%\QuarkRelay\`，覆盖或删除程序文件均不影响其中数据；
如需完全清理，删除该目录即可。

---

## 八、附：本版改动文件

| 文件 | 说明 |
| --- | --- |
| `quarkrelay/core/updater.py` | 替换脚本新增删旧版本 / 清安装包 / 启动确认 / 写记录；新增 `cleanup_after_update()` 兜底清理 |
| `quarkrelay/app.py` | 启动时调用兜底清理，并把清理结果提示给用户 |
| `quarkrelay/ui/pages/about.py` | 更新说明与「立即更新」提示文案 |
| `scripts/selftest.py` | 更新一项新增 4 组断言（旧安装包、日志记录、兜底清理、进行中跳过） |
| `quarkrelay/__init__.py` | 版本号 1.1.2 → 1.1.3 |
| `README.md` / `FEATURES.md` / `RELEASE_NOTES.md` | 文档同步 |

---

## 免责声明

本工具只用于管理你本人账号中的文件，以及转存他人公开分享的合法内容。
请勿用于传播盗版、侵权或任何违法内容。使用本工具产生的一切后果由使用者自行承担。
