# 夸克中转站 v1.1.0

**发布日期：2026-09-13**

这一版解决三件让你用不下去的事：**夸克登不上**、**登录信息读不全**、**只能单向搬运**，
顺手把「检查更新」做成了真正的一键自动更新。

---

## 🔧 重要修复：夸克网盘登录不了

之前点「登录夸克网盘」，扫码、点同意，然后就没有然后了。两个原因叠在一起：

1. **请求签名算错了** —— 夸克开放平台的签名只覆盖「方法 + 路径 + 时间戳 + 密钥」，
   而旧代码把 `?page_code=xxx` 这部分查询串也一起签了进去，服务端直接判定
   `errno 10001 签名验证失败`。现在签名统一只算路径，这个坑不可能再踩。
2. **构造错误对象时参数名写错** —— 抛异常时用了 `AppError(errno=...)`，而这个参数并不存在，
   一进轮询就抛 `TypeError` 直接崩掉，日志里只有一个看不懂的类型错误。

同时把授权流程改稳了：

* 授权结果由**后台线程统一轮询**（等授权 → 换令牌 → 取用户信息一条龙），
  窗口不再是「点一下就当失败」；
* 授权期间接口返回「还没授权」不再被当成错误，状态栏会同步显示
  「已扫码，等待你点击同意授权」这类实时提示；
* 拿到授权码立即自动关闭窗口并保存登录状态，不需要你再点「我已授权完成」。

## 🔧 重要修复：登录信息读取不到位

* 读 Cookie 从「固定等 0.9 秒」改成**等回调流真正静默下来**（最少 700ms、连续 400ms 没新
  Cookie 才收工，最长 8 秒）。Cookie 一多就不会再漏读，
  `BDUSS` / `STOKEN` 这类 **HttpOnly** 的关键项也拿得全。
* 建立百度会话时**按域名取 Cookie**。`BDUSS` 在 `.baidu.com` 和 `pan.baidu.com` 下可能
  是两个不同的值，以前混在一起互相覆盖，登录看着「成功」了但接口就是不通。
* 修掉了 WebEngine 缓存目录不存在导致的 `Unable to move the cache`，顺手把目录预建好。

---

## 🚀 新增：跨盘搬运支持双向

「百度 → 夸克」页面升级成 **跨盘搬运** 页，顶部一个方向开关：

* **百度 → 夸克**：解析分享 → 勾选文件 → 转存到你的百度中转目录 →
  内置浏览器截获直链 → 流式下载 → 上传夸克 → 可选生成夸克分享链接
* **夸克 → 百度**：解析分享（整包预览）→ 转存到你的夸克中转目录 → 流式下载 →
  上传百度网盘（同名文件自动加序号，也可选择跳过或覆盖）→ 可选生成百度分享链接

两个方向都不经过桌面：文件只在 `%APPDATA%\QuarkRelay\cache\buffer\` 里过一手，
任务结束随用随删（想留着可以勾「保留下载缓冲文件」）。

## 🚀 新增：一键自动更新

「关于」页现在是完整的更新中心：

* 读取 GitHub 上的**最新 Release**，和本地版本按数字段比对（`1.10.0` 比 `1.9.9` 新）
* 显示新版本号、**安装包大小**、发布日期，并把该版本的更新公告单独列一个页签
* 点「立即更新到 vX.Y.Z」→ 后台带进度下载 → 自动校验（大小 + `MZ` 文件头 + 可选的 sha256）
  → 生成替换脚本 → **重启后就是新版本**，全程不用去网页下载
* 默认**启动时静默检查**；发现新版本且当前没有任务在跑时，会**自动下载并安装**
  （有任务在跑就推迟，绝不打断你正在搬的东西，可在设置页关掉）
* 装在 `Program Files` 这类不可写目录时不会硬来，退化成「去下载页」按钮

## 🚀 改进：截图识链不再是独立页面

截图识链以前占了一个侧边栏页面，你得先在那边截好、再「送到夸克中转站」，链路太长。
现在它变成了一个 **18px 的相机小图标**，直接长在需要输入链接的页面上：

* **夸克中转页**右上角：截图识别到的夸克链接直接填进输入框
* **跨盘搬运页**右上角：按当前方向识别百度或夸克链接，自动填好链接与提取码

左键点一下就是**框选识别**（最常用），右键弹出「整屏识别 / 读剪贴板 / 打开图片…」。
全局快捷键 `Ctrl+Alt+Q` 和托盘菜单里的「截图识链」现在都作用于**当前页面**，
在哪一页就识别哪种链接。

---

## ✨ 其它改动

* 设置页新增「版本更新」区：启动自动检查、自动下载安装两个开关 + 「立即检查更新」
* 自检脚本从 10 项扩到 11 项，新增「检查更新：版本比较与降级路径」，断网也不会误报失败
* 补上百度网盘的上传实现（预上传秒传 → 4MB 分片 → 收尾）与分享创建（`share/set`）

## 📦 安装

下载本页附件里的 `QuarkRelay.exe`，双击运行即可。已经装了旧版本的，
放到同一位置覆盖，或者直接在程序里点「关于 → 检查更新」。

数据全部保存在 `%APPDATA%\QuarkRelay\`，卸载时直接删掉这个目录即可。

## ⚠️ 已知限制

* 百度搬运链路依赖百度网盘网页版结构；改版时可能需要你手点一下「下载」按钮
* 百度分享里的**目录**暂不支持整目录搬运，请勾选具体文件
* 夸克 → 百度方向是**整包转存**（夸克分享的勾选暂不支持），搬运前可以在预览里确认内容
* 自动替换依赖程序目录可写；装在需要管理员权限的目录时会提示手动下载
* 阿里云盘 / 123 云盘 / 天翼等链接已经能识别分类，但转存还没接通，后续版本补上

## 🙏 说明

本工具只用于管理你自己账号里的文件、转存他人公开分享的合法内容。
请勿用于传播盗版或侵权内容。
