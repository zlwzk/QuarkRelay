# 夸克中转站 v1.1.5 更新公告

| 项目 | 内容 |
| --- | --- |
| 发布日期 | 2026-09-14 |
| 上一版本 | v1.1.4 |
| 适用平台 | Windows 10 / 11（64 位） |
| 安装包 | `QuarkRelay.exe`（单文件免安装，约 262 MB） |
| 升级方式 | 覆盖旧文件，或在程序内「关于 → 检查更新 → 立即更新」 |

## 摘要

本版修复「**跨盘搬运**」这条链路的三处问题：解析百度分享必定失败（点「解析」就报「参数错误」）、
夸克 → 百度方向**显示上传成功但一个字节都没传**、以及报错文案会把百度说的真实原因盖掉。
三处都跟百度最近改动网页接口有关，登录凭据无需重做。

* 修复解析百度分享失败：百度给「读取分享列表」的接口加了参数要求，缺一个标记就统一报错；
* 修复夸克 → 百度上传假成功：预上传接口改了返回值的含义，老代码据此跳过了整个上传；
* 报错改成优先显示百度自己的说法，不再被本地错误表翻译成误导性的「参数错误」；
* 不涉及配置项、历史记录、登录凭据的存储格式变更，可直接覆盖升级；
* 自检仍为 13 项（本版未新增断言）。

---

## 一、缺陷修复

### 1.1 跨盘搬运：「解析失败：参数错误」（百度 → 夸克方向）

**症状**

在「跨盘搬运」页选「百度 → 夸克」粘贴百度分享链接，点「解析」直接弹「解析失败：参数错误」。

**原因**

百度给读分享列表的接口 `/share/list` 加了一个判定：**这次要列的是不是分享根目录**。

* 列分享**根目录**时必须带 `root=1`，不带就一律返回 `errno 2`，百度自己的原话是
  「啊哦，链接出错了」；
* 本地错误表把 `errno 2` 统一翻译成「参数错误」，于是真实原因被盖住，看着像参数写错了；
* 列**子目录**时反而不能带 `root=1`——带了会被忽略，永远返回根目录的内容。

**修法**

列分享根目录时补上 `root=1`，列子目录时不带；提取码校验后的 `sekey` 不用手动处理，
百度在 `/share/verify` 时已经把 `BDCLND` 写进会话 cookie 了。

### 1.2 夸克 → 百度：显示上传成功，其实一个字节都没传

**症状**

「夸克 → 百度」方向任务显示全部成功，但去百度网盘里根本找不到那些文件；
日志里只有一句「百度秒传命中」。

**原因**

上传分三步：预上传（拿上传会话）→ 传分片 → 收尾。老代码用预上传返回的 `return_type`
判断要不要传数据：`return_type != 2` 就当作「秒传命中」直接返回。而百度现在**一律返回 1**，
于是代码跳过了传分片、也跳过了收尾，还顺手捏了一个 `fs_id=0` 的假对象当结果——
任务因此「成功」，网盘里却什么都没有。

**修法**

* 不再看 `return_type`，只认有没有上传会话：有就传分片并收尾，传哪些分片按接口返回的
  `block_list` 来；
* 收尾结果以收尾接口的返回为准（里面有 `fs_id`），拿不到就回查一次网盘列表，
  还是找不到就明确报错——**不再伪造成功**；
* 万一没有上传会话（真秒传），也必须回查确认文件在网盘里存在，否则报错。

### 1.3 报错文案：让百度自己说话

原来的规则是「错误码在本地表里就用表里的文案」，结果转存失败时百度说的是
「文件已存在」，却被翻译成「参数错误」，把真正的原因盖掉了。现在**百度自己的说法优先**，
本地错误表只做兜底（比如百度什么文案都没给时）。

---

## 二、新增或界面调整

本版无新增功能与界面调整。

---

## 三、其它改动

* `quarkrelay/core/baidu.py`：`list_share` 补根目录标记；`upload` 的上传判定与收尾校验重写；
  `_errno_message` 的文案优先级调整；
* 其余调用点（搬运流水线、界面层）的接口签名与返回结构都没变，不用改。

---

## 四、兼容性

* 配置项、历史记录、登录凭据的存储格式本版未变更，夸克 / 百度登录都无需重做；
* 「跨盘搬运」的解析结果与任务流转格式不变，历史记录不受影响。

---

## 五、验证情况

* 程序自检 13 项全部通过；
* 用真实百度账号实测（经应用自己的代码路径）：
  * 解析带提取码的分享：修复前报「参数错误」，修复后正常列出内容；
  * 列分享内的子目录：正常；
  * 上传落地：修复前显示成功但网盘里搜不到，修复后文件确实落盘（`fs_id` 正常）；
  * 错误文案：转存冲突显示百度原话「文件已存在」，失效分享显示「该分享已被取消」；
  * 排查期间产生的测试目录与测试分享已全部清理（目录已删除、分享已取消）。

---

## 六、已知限制（本版未处理）

* 百度这套网页接口随时可能再改，遇到再适配；
* 若你把**自己网盘里**的文件做成分享、再用同一账号转存回来，百度会直接回「文件已存在」
  并拒绝转存（实测确认，与本地代码无关）；链接是别人分享给你的则不受影响；
* 其余限制与 v1.1.4 相同：百度分享里的目录不支持整目录搬运、截图识别依赖 Windows
  中文语言包等。

---

## 七、升级方式与数据位置

1. 程序内：关于 → 检查更新 → 立即更新（自动下载、校验、替换、清理并重启）。
2. 手动：下载本页附件的 `QuarkRelay.exe`，覆盖旧文件即可。

数据保存在 `%APPDATA%\QuarkRelay\`，覆盖或删除程序文件均不影响其中数据；
如需完全清理，删除该目录即可。

---

## 八、附：本版改动文件

| 文件 | 说明 |
| --- | --- |
| `quarkrelay/core/baidu.py` | 分享列表补 `root=1`；上传判定与收尾校验重写，去掉伪造成功；报错文案优先用百度原文 |
| `quarkrelay/__init__.py` | 版本号 1.1.4 → 1.1.5 |
| `RELEASE_NOTES.md` | 本公告 |

---

## 免责声明

本工具只用于管理你本人账号中的文件，以及转存他人公开分享的合法内容。
请勿用于传播盗版、侵权或任何违法内容。使用本工具产生的一切后果由使用者自行承担。
