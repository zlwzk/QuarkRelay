# 夸克中转站 v1.1.8 更新公告

| 项目 | 内容 |
| --- | --- |
| 发布日期 | 2026-09-15 |
| 上一版本 | v1.1.7 |
| 适用平台 | Windows 10 / 11（64 位） |
| 安装包 | `QuarkRelay.exe`（单文件免安装，约 262 MB） |
| 升级方式 | 覆盖旧文件，或在程序内「关于 → 检查更新 → 立即更新」 |

## 摘要

**本版是维护版：功能、界面、行为与 v1.1.7 完全一致，业务代码零改动。**

起因是把「UI 数值输入规范」固化成了团队开发规则（新增
`.codebuddy/rules/ui-numeric-input/RULE.mdc`）：时间 / 百分比 / 次数类参数必须用原生数值控件、
输入框内只放半角数字、单位（KB/s、秒、次）一律放框外独立标签、禁止符号占位与模板文本填 `value`。

顺手把本项目的界面按这条规范**逐项复核**了一遍，结论是**原本就全部合规**，无需改代码：

* 并行任务数、限速这两处数值参数本来就是 `QSpinBox`，说明文字写在输入框**外面**的标签上
  （「限速（KB/s，0 为不限）」这种写法是标签，不是 `setSuffix`），框内只有数字；
* 全项目**没有**使用 `setSuffix` / `setPrefix` 把单位塞进输入框；
* 界面里其余的 `QLineEdit` 都是文本/路径/链接/搜索框（分享链接、提取码、目录名、命名模板、
  历史搜索、直链粘贴），按规范它们本来就该是文本框，不属于数值输入；
* 没有罗马数字 / 全角圆圈之类的符号占位，默认值都是有效数字。

所以本版**唯一的变化就是新增开发规则文件 + 版本号 1.1.7 → 1.1.8**，
目的是让版本号与仓库状态保持一致。对使用者来说**没有必须升级的理由**：
不升级也能继续正常用，程序内「检查更新」也不会催你。

---

## 一、本版改动

| 文件 | 说明 |
| --- | --- |
| `.codebuddy/rules/ui-numeric-input/RULE.mdc` | 新增开发规则：UI 数值输入规范（原生数值控件 / 框内纯数字 / 单位外置 / 禁止符号占位 / 禁止模板文本填 `value`） |
| `quarkrelay/__init__.py` | 版本号 1.1.7 → 1.1.8 |
| `README.md` | 版本行同步（原先停留在 v1.1.3，一并修正） |
| `RELEASE_NOTES.md` | 本公告 |

`quarkrelay/core/**`、`quarkrelay/ui/**`、`scripts/**` **全部未改动**。

---

## 二、界面数值输入的复核结论

| 位置 | 控件 | 单位写法 | 结论 |
| --- | --- | --- | --- |
| 设置 → 并行任务数 | `QSpinBox`（1~6） | 说明在左侧标签 | 合规：框内只有数字 |
| 设置 → 限速 | `QSpinBox`（0~102400，步进 256） | 左侧标签「限速（KB/s，0 为不限）」 | 合规：单位在框外 |
| 提取码 / 分享链接 / 分享名字 | `QLineEdit` | — | 合规：本就是文本输入 |
| 目录、命名模板、历史搜索 | `QLineEdit` | — | 合规：本就是文本输入 |

复核方式：全局搜索 `setSuffix` / `setPrefix` / `setPlaceholderText` / `QSpinBox` / `QLineEdit`，
逐个确认控件类型与单位位置。

---

## 三、兼容性

* 配置项、历史记录、登录凭据的存储格式本版**未变更**，夸克 / 百度登录都无需重做；
* 搬运设置、命名模板、默认目录等沿用原来的值，不需要重新配置；
* 从 v1.1.7 覆盖升级或原地自动更新均可，数据完全不受影响。

---

## 四、验证情况

* 程序自检 **13 项全部通过**（与 v1.1.7 相同的用例集，未增删）；
* 复核结论通过全局符号搜索得出，未改动任何业务代码，因此不存在回归面。

---

## 五、升级方式与数据位置

1. 程序内：关于 → 检查更新 → 立即更新（自动下载、校验、替换、清理并重启）。
2. 手动：下载本页附件的 `QuarkRelay.exe`，覆盖旧文件即可。

数据保存在 `%APPDATA%\QuarkRelay\`，覆盖或删除程序文件均不影响其中数据；
如需完全清理，删除该目录即可。

---

## 六、已知限制

* 「夸克 → 百度」方向仍搬不了整目录：开放平台没有目录列举接口，无法把目录里的文件搜回来。
  分享里只有文件夹时会明确提示，需要你把文件单独分享一次；
* 百度分享的目录展开最多 8 层，超出会报错停下（正常分享不会到这个深度）；
* 直链依赖内置浏览器（QtWebEngine）与当前登录态；自动点击失败仍需手动点一下。

---

## 免责声明

本工具只用于管理你本人账号中的文件，以及转存他人公开分享的合法内容。
请勿用于传播盗版、侵权或任何违法内容。使用本工具产生的一切后果由使用者自行承担。
