# 夸克中转站 v1.1.2

**发布日期：2026-09-13**

这一版把上一版没做干净的两件事补上了：**登录窗里只留二维码**，
以及**修好「扫码登录成功却依旧显示未登录」**。

---

## 🔧 百度：扫完码却一直说没登录 —— 两个叠在一起的 bug

上一版把二维码放大摆出来了，但百度这条链路其实走不通。两个毛病凑在一块儿：

1. **读不到 Cookie**。会话 Cookie（`BDUSS` / `STOKEN`）是 HttpOnly 的，只能从浏览器那侧取。
   原先用的 `QWebEngineCookieStore.loadAllCookies()`，在本机的 Qt 6.11 上**一条都回不出来**：
   Cookie 库里明明躺着 40 条、其中就有 `BDUSS`，回调却是空的。
   于是「浏览器里已经登录了」和「程序说你没登录」同时成立。
   现在改成两条腿走路：登录窗全程监听 `cookieAdded`（页面刚写下的 Cookie 立刻到手），
   窗口打开时再直接读一次 Profile 的 Cookie 库垫底 —— 冷启动时的旧会话也不会漏。
2. **Cookie 撞名**。建会话时把同一个 Cookie 挂到了 3 个域上
   （`.baidu.com` / `pan.baidu.com` / `.pan.baidu.com`，后两个其实是一个域），
   requests 读 `BDUSS` 时会直接抛 `CookieConflictError` —— 表现就是「扫了码还是未登录」。
   现在统一只挂 `.baidu.com` 这一个域，它本身就能覆盖 pan / passport / pcs 等全部子域。

顺带修掉一个连带问题：Cookie 库里的旧会话如果**已经失效**，窗口会刚打开就自己关掉、
让你根本没机会扫码；现在这种情况下会把它清掉，下次能正常出码。

> 已经扫过码的朋友：直接再点一次「登录百度网盘」就应该能进 —— 浏览器里那份会话
> 还在，程序现在读得到了，不用重新扫。

## 🎯 登录窗里只留二维码

上一版是「左边二维码 + 右边登录页」两栏并排，屏幕小的时候还是有点挤。
现在登录窗里**只有那一枚放大后的二维码**（白底卡片 + 静区），水平居中，
窗口宽度收到刚好装下卡片：

* 想自己看一眼或手动操作登录页，点底部的**「显示登录页」**就能展开，再点收回去
* 二维码取不到时（比如浏览器里已经登录、页面压根没码），提示语会直接告诉你点哪儿看
* 夸克同理：授权页也收起来了，只摆二维码

## ✨ 其它改动

* 自检从 12 项扩到 13 项，新增「登录会话：cookie 库读取与同名冲突」：
  造一个迷你 Cookie 库验证读取逻辑（过期/空值要跳过），并盯住「同名 Cookie 只能挂一个域」
* 窗口尺寸计算补了断言：只看二维码时要收窄到刚装得下卡片，展开登录页时才变宽

## 📦 安装

下载本页附件里的 `QuarkRelay.exe`，双击运行即可。已经装了旧版本的，放到同一位置覆盖，
或者直接在程序里点「关于 → 检查更新」。

数据全部保存在 `%APPDATA%\QuarkRelay\`，卸载时直接删掉这个目录即可。

## ⚠️ 已知限制

* 百度搬运链路依赖百度网盘网页版结构；改版时可能需要你手点一下「下载」按钮
* 百度分享里的**目录**暂不支持整目录搬运，请勾选具体文件
* 夸克 → 百度方向是**整包转存**（夸克分享的勾选暂不支持），搬运前可以在预览里确认内容
* 自动替换依赖程序目录可写；装在需要管理员权限的目录时会提示手动下载

## 🙏 说明

本工具只用于管理你自己账号里的文件、转存他人公开分享的合法内容。
请勿用于传播盗版或侵权内容。
