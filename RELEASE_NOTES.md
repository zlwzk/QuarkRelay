# 夸克中转站 v1.1.6 更新公告

| 项目 | 内容 |
| --- | --- |
| 发布日期 | 2026-09-14 |
| 上一版本 | v1.1.5 |
| 适用平台 | Windows 10 / 11（64 位） |
| 安装包 | `QuarkRelay.exe`（单文件免安装，约 262 MB） |
| 升级方式 | 覆盖旧文件，或在程序内「关于 → 检查更新 → 立即更新」 |

## 摘要

本版修掉自动更新时**满屏黑框**的问题：点完「立即更新」，替换过程中会一个接一个弹出
控制台窗口（实测一次更新能闪出十来个），像是有程序在后台乱跑。原因是替换脚本被以
「完全没有控制台」的方式启动，它里面每条命令都被系统各配了一个新控制台窗口。
改成「隐藏控制台」后，更新全程一个窗口都不会冒出来。

顺带修掉更新日志里那句误导人的「**没能自动打开**」误报：单文件 exe 更新后第一次启动
要先把自己解包出来（260 MB），慢的时候一分多钟才现身，而脚本只等了三秒就下结论。

* 更新过程不再出现任何控制台窗口（黑框），界面点完「立即更新」就是安静替换；
* 替换脚本等新版本现身的耐心从 3 秒延长到约 75 秒，并在日志里区分
  「新版本已打开」与「确实没能打开」；
* 自检从 13 项变为 14 项：新增一条断言，把更新的启动方式钉死在「隐藏控制台」，
  防止以后有人改回弹窗的写法；
* 不涉及配置项、历史记录、登录凭据的存储格式变更，可直接覆盖升级。

---

## 一、缺陷修复

### 1.1 每次更新都弹出一堆控制台窗口

**症状**

在「关于 → 检查更新 → 立即更新」之后，程序重启替换的那段时间里，会连续弹出若干个
黑色控制台窗口（有时只闪一下，有时停留一两秒），看起来像是有什么东西在偷偷跑命令。

**原因**

更新是在主程序退出后由一段批处理（`apply-update.bat`）完成替换的。为了不让用户看到
黑框，原来是用 `DETACHED_PROCESS` 拉起这段脚本的——但它的含义是「这个 cmd **完全没有**
控制台」，而不是「有控制台但藏起来」。于是脚本里每调用一个控制台程序
（`tasklist`、`find`、`ping`），Windows 都要给它**新开**一个控制台窗口：

* 等旧进程退出的循环里，每轮 3 条命令；
* 替换失败的重试循环里，每轮 1 条 `ping`；
* 启动新版本后的确认循环里，每轮 2 条命令。

脚本里这些循环是「按需要重试」的，所以窗口数量不固定——这也解释了为什么有时是几个、
有时是一长串。

**修法**

改用 `CREATE_NO_WINDOW` 启动替换脚本：cmd 自己带一个「没有窗口」的控制台，
脚本里所有命令都继承它，因此一个可见窗口都不会创建。启动方式提成模块常量
`updater.STARTUP_FLAGS`，并在自检里加了断言看住它（见 1.2）。

**验证**（同一段脚本、两种启动方式对比，实验脚本模拟真实脚本的关键命令）

| 启动方式 | 新出现的可见控制台窗口 | `tasklist` / `find` / `start` 是否正常 |
| --- | --- | --- |
| `DETACHED_PROCESS`（旧） | **10 个** | 正常 |
| `CREATE_NO_WINDOW`（新） | **0 个** | 正常（新版本被正常拉起） |

### 1.2 更新日志误报「没能自动打开」

**症状**

`%APPDATA%\QuarkRelay\update\update.log` 里每次更新都写着：

```
替换完成：旧版本文件与安装包已清理，接下来打开 QuarkRelay.exe
已替换成新版本，但没能自动打开，请手动双击 "…\QuarkRelay.exe"
```

看着像自动重启失败了，其实新版本**已经起来了**（应用日志里能看到更新后 40 秒 / 2 分钟
各有一条正常启动记录）。也就是说：功能没问题，是虚惊一场的记录。

**原因**

脚本用 `tasklist` 确认新版本是否起来，只重试 3 次（约 9 秒）。而单文件 exe 更新后
第一次启动要先把自己解包到临时目录（260 MB 上下），冷启动慢的时候一分多钟才出现在
进程列表里，于是 3 次都没等到就写了这条记录。

**修法**

* 重试上限从 3 次提到 30 次（约 75 秒），足够覆盖冷启动；
* 成功后明写一条「新版本已打开」，失败时把 `start` 的返回码一起记下来，
  下次真出问题能一眼分清是「没能启动」还是「起得慢」；
* 失败文案也改成「等了约一分钟没能自动打开」，不再让人以为必须手动双击。

---

## 二、新增或界面调整

本版无新增功能与界面调整，界面行为上只有一处变化：自动更新期间不再弹控制台窗口。

---

## 三、其它改动

* `quarkrelay/core/updater.py`：新增 `STARTUP_FLAGS`（`CREATE_NO_WINDOW`）并用于启动替换脚本；
  替换脚本的启动确认循环改为耐心等待（约 75 秒），日志区分「已打开 / 没能打开」；
* `scripts/selftest.py`：更新流程的用例改用真实启动方式，并新增一条断言看住
  「不能用 `DETACHED_PROCESS`」；
* 替换脚本的替换、回滚、清理逻辑本身没有改动，行为与 v1.1.5 一致。

---

## 四、兼容性

* 配置项、历史记录、登录凭据的存储格式本版未变更，夸克 / 百度登录都无需重做；
* 更新脚本的文件名、位置、日志格式都没变，从 v1.1.5 及更早版本升级的路径一致；
* 装在 `Program Files` 等不可写目录时仍然退化为手动下载，行为与之前相同。

---

## 五、验证情况

* 程序自检 14 项全部通过（新增 1 项：更新脚本的启动方式）；
* 对照实验：同一段替换脚本分别用 `DETACHED_PROCESS` 与 `CREATE_NO_WINDOW` 启动，
  前者冒出 10 个可见控制台窗口，后者 0 个；两种情况下 `tasklist` 查找、
  `find` 过滤、`start` 拉起 GUI 程序都正常；
* 用真实机器的更新日志复核了「没能自动打开」属于误报：更新后新版本分别在
  40 秒、2 分钟后正常启动；
* 自检里仍然真跑一遍替换脚本（等进程退出 → 替换 → 清理 → 脚本自删），
  确认改动没有破坏替换与收尾流程。

---

## 六、已知限制（本版未处理）

* 更新后第一次启动仍要解包 260 MB，慢机器上等半分钟属正常（不是卡住）；
* 替换脚本为了兼容各种中文路径与占用情况，仍会在后台反复重试若干次，
  这些动作现在完全不可见，但会占用几秒钟；
* 其余限制与 v1.1.5 相同：百度分享里的目录不支持整目录搬运、截图识别依赖 Windows
  中文语言包等。

---

## 七、升级方式与数据位置

1. 程序内：关于 → 检查更新 → 立即更新（自动下载、校验、替换、清理并重启）。
2. 手动：下载本页附件的 `QuarkRelay.exe`，覆盖旧文件即可。

数据保存在 `%APPDATA%\QuarkRelay\`，覆盖或删除程序文件均不影响其中数据；
如需完全清理，删除该目录即可。

---

## 八、附：本版改动文件

| 文件 | 说明 |
| --- | --- |
| `quarkrelay/core/updater.py` | 替换脚本改用隐藏控制台启动；启动确认改为耐心等待并区分日志 |
| `quarkrelay/__init__.py` | 版本号 1.1.5 → 1.1.6 |
| `scripts/selftest.py` | 更新用例改用真实启动方式；新增启动方式断言（13 → 14 项） |
| `RELEASE_NOTES.md` | 本公告 |

---

## 免责声明

本工具只用于管理你本人账号中的文件，以及转存他人公开分享的合法内容。
请勿用于传播盗版、侵权或任何违法内容。使用本工具产生的一切后果由使用者自行承担。
